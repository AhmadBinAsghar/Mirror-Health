export enum MonitoringConstants {
    heartRate = 20,
    hrv = 10,
    spo2 = 15,
    temperature = 17,
}

export enum MonitoringContForCalibration {
    heartRate = 15,
    hrv = 5,
    spo2 = 7,
    temperature = 10,
}